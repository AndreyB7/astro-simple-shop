---
interface Props {
  class?: string;
}

const baseClass = "text-2xl text-center md:text-5xl font-semibold uppercase leading-tight md:leading-tight";

const { class: className = baseClass } = Astro.props;
---

<h2 class={className} data-aos="fade-up"><slot /></h2>
